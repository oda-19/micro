spring:
  application:
    name: gateway-server

  config:
    import: optional:configserver:http://localhost:8071

  cloud:
    compatibility-verifier:
      enabled: false
logging:
  level:
    com.netflix: WARN
    org.springframework.web: WARN
    com.example: DEBUG